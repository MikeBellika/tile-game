import { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
  appId: "dk.bellika.exponentile",
  appName: "ExponenTile",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
}

export default config
